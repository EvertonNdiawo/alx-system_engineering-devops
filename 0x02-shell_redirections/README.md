#In this project, i'm going to create scripts to execute shell redirections and provide descriptions on what each script does.

0-hello_world -> A script that prints "Hello world" to the screen followed by a newline character. 

1-confused_smiley -> A script that displays a confused smiley "(Ôo)' 

2-hellofile -> A script that displays the content of the /etc/passwd file

3-twofiles -> Scrpt to display contents of /etc/passwd and /etc/hosts

4-lastlines -> A script to display the last ten lines of /etc/passwd.

5-firstlines -> Script to display the first ten lines of /etc/passwd.

6-third_line -> Script to display the third line of the file iacta located in the working directory.

7-A shell script that creates a file named exactly \*\\'"Best School"\'\\*$\?\*\*\*\*\*:) which contains the text Best School ending by a new line.

8-cwd_state -> script that writes into the file ls_cwd_content the result of the command ls -la, overwritting it if it exists and creating it if it does not exist.

9-duplicate_last_line -> A script that duplicates last line of the file iacta, assumed to be in the working directory.

10-no_more_js -. script that deletes all regular files and not directories with a .js extension that are present in the current directory and all its subfolders

11-directories -> script that counts the mumber of directories and sub-directories in the current directory, not taking into account the current and parent diectories and also counting hidden directories.

12-newest_files -> A script to display the 10 newest files in the current directory, one file per line, sorted from newest to oldest.

13-unique -> A Script that gets a list of words as input and prints only words that appear exaclty once.

14-findthatword -> A script to display lines contaiing the pattern "root" from the file /etc/passwd.

15-countthatword -> A Script to display the number of lines that contain the pattern "bin" in the file /etc/passwd.

16-whastsnext -> A script to display lines containing the pattern "root" and 3 lines after them in the file /etc/passwd.

17-hidethisword -> A script to display all the lines in the file /etc/passwd that do not contain the pattern "bin".

18-letteronly -> A script to display all the lines of the file /etc/ssh/sshd_config starting with a letter. To include capital letters as well. 

19-AZ -> A script to Replace all characters A and c from input to Z and e respectiveluy.

20-hiago -> A script that removes all letters c and C from input.

21-reverse -> A script that reverses its input.

22-users_and_homes -> A script that displays all users and their home directories, sorted by users. based on the file /etc/passwd.


